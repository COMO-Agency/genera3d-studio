const ModelViewer = ({ url: _url }: { url: string }) => {
  return (
    <div className="aspect-[4/3] rounded-lg bg-muted flex items-center justify-center text-muted-foreground text-sm">
      3D-Vorschau nicht verfügbar
    </div>
  );
};

export default ModelViewer;
